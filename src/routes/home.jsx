import React from 'react'
import {
    Form,
    FormGroup,
    Label,
    Input,
    FormText,
    Alert,
} from 'reactstrap';

const Home = () => {
    return (

        <Form>
            <Alert variant="primary">
                DASHBOARD DA HOME
            </Alert>
        </Form>

    )
}

export default Home

